# MeshForge pinned environment — Windows 11 / Python 3.12 / RTX 4060.
# Pinned 2026-06-12 from the verified working environment (direct deps are
# curated in requirements.in). Torch stack is CUDA 12.4 ("cu124"); all
# torch-coupled wheels below must move together with the torch version.
#
# Two packages have NO Windows wheels on any index and are built from source
# with the ninja + MSVC toolchain (see README "Memory" section):
#   pip install "git+https://github.com/facebookresearch/pytorch3d.git@V0.7.9"   # -> 0.7.9+pt2.6.0cu124
#   pip install "git+https://github.com/NVlabs/nvdiffrast.git@v0.3.3"            # -> 0.3.3

--extra-index-url https://download.pytorch.org/whl/cu124
--find-links https://nvidia-kaolin.s3.us-east-2.amazonaws.com/torch-2.6.0_cu124.html
--find-links https://data.pyg.org/whl/torch-2.6.0%2Bcu124.html

# torch stack (coupled)
torch==2.6.0+cu124
torchvision==0.21.0+cu124
xformers==0.0.29.post3
kaolin==0.18.0
torch-scatter==2.1.2

# avatar_pipeline direct imports
accelerate==1.13.0
diffusers==0.36.0
huggingface-hub==0.36.2
insightface==0.7.3
mmgp==3.7.6
numpy==2.4.4
omegaconf==2.3.0
opencv-python==4.11.0.86
pillow==12.2.0
pymeshlab==2025.7
rembg==2.0.69
scipy==1.17.1
trimesh==4.5.3
typer==0.24.2

# required by the vendored external/ repos at runtime
basicsr==1.4.2
einops==0.8.0
embreex==4.4.0
onnxruntime-gpu==1.26.0
open3d==0.19.0
peft==0.19.1
realesrgan==0.3.0
safetensors==0.8.0rc0
scikit-image==0.24.0
smplx==0.1.28
transformers==4.54.0
xatlas==0.0.11

# dev
pytest==9.0.2
